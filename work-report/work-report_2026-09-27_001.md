# Work report — 2026-09-27

## Pi clipboard in SSH/tmux sessions

- Confirmed Pi could read the Fedora Wayland image clipboard when launched with `WAYLAND_DISPLAY=wayland-1`, but not when started without that variable over SSH.
- Added `scripts/pi-with-clipboard.sh` to discover one Wayland socket, preserve explicit socket selection, and fail clearly if no socket or multiple sockets are available. Pi profiles do not need the Codex wrapper's XWayland setup.
- Updated the four Pi profile aliases in `/home/afwcxx/Dropbox/config/bash_profile_desktop` to use the launcher while retaining their existing agent directories and provider.
- Added `scripts/pi-with-clipboard.test.sh` and registered it in `package.json`.

## Validation

- `bash scripts/pi-with-clipboard.test.sh` and existing Codex wrapper tests passed.
- `pnpm test` passed; the shared package has no tests and reports that explicitly.
- `pnpm format:check`, `zsh -n /home/afwcxx/Dropbox/config/bash_profile_desktop`, and `git diff --check` passed.
- `env -u WAYLAND_DISPLAY scripts/pi-with-clipboard.sh --version` returned `0.87.1` with the active Wayland socket.
- User still needs to start a fresh shell or reload the profile and confirm `Ctrl+V` from a new SSH Pi profile session.

Existing unrelated `docker-compose.yml` changes were left untouched.
